In an NGO many applications come. Apllications ID's are from A-Z nd a-z. Voulteers ID's have( a,e,i,o,u,A,E,I,O,U). Intern's ID's have remaining aplahbets.
Find count of Interns applications received.
INPUT :  9
         buybxcnsi
OUTPUT : 2
--------------------------------------------------------------------------------------

import java.util.*;
class TestClass {
    public static void main(String args[] ) {
        Scanner s=new Scanner(System.in);
        int cnt=0;
        int n=s.nextInt();
        String s1=s.next();
        String s2="";
        for(int i=0;i<n;i++){
            s2=s1.toUpperCase();
           }
      // System.out.println(s2);
        for(int i=0;i<n;i++){
        char ch=s2.charAt(i);
        if(ch=='A' || ch=='E' || ch=='I' || ch=='O' || ch=='U'){
            cnt=cnt+1;
        }
    }
    System.out.println(n-cnt);
    }
}
